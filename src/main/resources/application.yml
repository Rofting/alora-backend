spring:
  application:
    name: alora-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/alora
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: public

server:
  port: 8080